/*import dBconnexion from "../../../backend/utils/dbconnect";

export async function getUser(id) {
    const user = await dBconnexion `SELECT * FROM uservalidations WHERE id = ${id}`;
    return user;
  }
    */

  // API route to get users from the database
